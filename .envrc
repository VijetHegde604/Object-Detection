# Direnv + nix-direnv setup
# Default: CPU shell
# For CUDA shell, run: NIX_SHELL_FLAVOR=cuda direnv reload

if has nix_direnv_version; then
  if [ "${NIX_SHELL_FLAVOR:-default}" = "cuda" ]; then
    use flake .#cuda
  else
    use flake
  fi
else
  use flake
fi
